# Student-facing release only — everything else in this folder stays local.

# Ignore everything at the top level...
/*

# ...except the published materials:
!/.gitignore
!/README.md
!/AI_in_Industry_and_Business_Workshop_Deck.pdf

# The complete source pack (except editable originals)
!/source_pack/
/source_pack/03_Filled_AI_Opportunity_Canvas_Production_Shift_Handover_A3.docx

# Course notes: PDFs only (markdown sources stay local)
!/student_course_notes/
/student_course_notes/*
!/student_course_notes/*.pdf
